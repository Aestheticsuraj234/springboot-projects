services:
  postgres:
    image: postgres:17
    container_name: v0-postgres

    restart: unless-stopped

    environment:
      POSTGRES_DB: v0
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres

    ports:
      - "5435:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d v0"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data: